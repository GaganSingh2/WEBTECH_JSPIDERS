a = 20 //By default become var type variable
console.log(a); //20
var a;

// b = 40 //ReferenceError(not possible to access before initialization)
// bcz If a variable is already initialized with some value, then only you can access it before the line in which it is declared.
let b
console.log(b); //undefined(default value for the var)

b = 30
console.log(b); //30





